<!-- model: claude-sonnet-4-5-20250929 -->

## Summary: Feature ID on Agent Output

### Changes Implemented

- Moved the feature ID badge to be inline with the description text on the same line
- Changed from `mt-2` (top margin, separate line) to `ml-2` (left margin, same line) with `align-middle` for vertical alignment
- Feature ID is now always rendered adjacent to the description text

### Files Modified

- `apps/ui/src/components/views/board-view/dialogs/agent-output-modal.tsx`

### Notes for Developer

- The feature ID badge now appears inline within the description div, immediately after the description text
- Uses `align-middle` to vertically align the badge with surrounding text
- The "Show more/less" toggle still works independently below the description line
