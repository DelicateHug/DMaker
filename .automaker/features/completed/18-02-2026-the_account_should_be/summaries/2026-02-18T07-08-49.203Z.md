<!-- model: claude-opus-4-6 -->

## Summary: Account Switcher Dropdown in Usage Popover

### Changes Implemented

- Added an `AccountSwitcherDropdown` component to `usage-popover.tsx` that displays the current Claude account email as a clickable dropdown
- The dropdown shows all saved Claude accounts with a checkmark next to the active one
- Non-active accounts have a delete (X) button to remove them from the saved list
- A "Add new account" option at the bottom triggers the `claude login` re-authentication flow
- Selecting a different account shows a toast instructing the user to run `claude login` in their terminal and then triggers re-authentication
- Replaced the static email text in the Claude tab header with the interactive `AccountSwitcherDropdown`
- Added `handleAccountSwitch` callback to the `UsagePopover` component

### Files Modified

- `apps/ui/src/components/usage-popover.tsx` - Added `AccountSwitcherDropdown` component, `handleAccountSwitch` callback, and replaced static email display with dropdown

### Notes for Developer

- All required imports (`DropdownMenu*`, `ChevronDown`, `Check`, `X`, `UserPlus`, `ClaudeAccountRef`) were already present in the file
- The app store already had `claudeAccounts` and `removeClaudeAccount` wired up and being pulled in by `UsagePopover`
- Account emails are auto-tracked when usage data is fetched (via `addOrUpdateClaudeAccount` in `setClaudeUsage`)
- The `claude-usage-popover.tsx` file already had an identical implementation but wasn't being used in the actual app (only `usage-popover.tsx` is rendered via `top-nav-bar.tsx` and `board-header.tsx`)
- No new TypeScript errors were introduced; all pre-existing errors are unrelated
